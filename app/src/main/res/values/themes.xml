<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Uygulama içi ekranlar (Compose). -->
    <style name="Theme.RfidTablet" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds" tools:targetApi="l">true</item>
        <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
        <item name="android:enforceStatusBarContrast" tools:targetApi="29">false</item>
    </style>

    <!-- Soğuk açılış: koyu zemin, beyaz vektör; inset yok (orijinal oran). -->
    <style name="Theme.RfidTablet.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_takipsan_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.RfidTablet</item>
    </style>
</resources>
